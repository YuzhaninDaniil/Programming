<UserControl x:Class="ContactCollection.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContactCollection.Controls" xmlns:converters="clr-namespace:ContactCollection.Converters" xmlns:viewmodel="clr-namespace:ViewModel;assembly=ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:MainVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BooleanInverseConverter x:Key="BooleanInverse"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel Grid.Column="2" Background="White" >
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="3"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Margin="0, 19, 0, 0">
                    <Image Width="100" Height="100" VerticalAlignment="Top" Source="C:\Users\скда\source\repos\Project333\jFLd-SJBw7Y.jpg"/>
                </Grid>

                <StackPanel Grid.Column="3" Grid.Row="0">
                    <Label Content="Name:"/>
                    <TextBox x:Name="NameTextBox" IsReadOnly="{Binding IsEditMode, Converter={StaticResource BooleanInverse}}" Margin="0, 3, 0, 3">
                        <TextBox.Text>
                            <Binding Path="SelectedContact.Name" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <DataErrorValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    <Label Content="Phone number:"/>
                    <TextBox x:Name="PhoneTextBox"  IsReadOnly="{Binding IsEditMode, Converter={StaticResource BooleanInverse}}" Margin="0, 3, 0, 3" PreviewTextInput="PhoneNumberTextBox_PreviewTextInput" DataObject.Pasting="PhoneNumberTextBox_Pasting">
                        <TextBox.Text>
                            <Binding Path="SelectedContact.PhoneNumber" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <DataErrorValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                    <Label Content="Email:"/>
                    <TextBox x:Name="EmailTextBox" IsReadOnly="{Binding IsEditMode, Converter={StaticResource BooleanInverse}}" Margin="0, 3, 0, 3">
                        <TextBox.Text>
                            <Binding Path="SelectedContact.Email" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <DataErrorValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
