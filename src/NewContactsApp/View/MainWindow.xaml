<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        xmlns:vm="clr-namespace:View.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Icon="contacts_96x96.ico">
    
    <Window.DataContext>
        <vm:MainVM />
    </Window.DataContext>

    <Window.Resources>
        <vm:BoolToVisibilityConverter x:Key="BooleanToVisibility"/>
        <vm:BooleanInverseConverter x:Key="BooleanInverse"/>
    </Window.Resources>

    <DockPanel Margin="15">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Background="#f8f8f8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Find:" Margin="-3,3,3,3" VerticalAlignment="Center"/>
                    <TextBox x:Name="SearchTextBox" Grid.Column="1" Margin="3" VerticalAlignment="Center"/>
                </Grid>

                <ListBox x:Name="ContactsListBox" Grid.Row="1" Margin="8,8,8,8"
                     ItemsSource="{Binding Contacts}"
                     SelectedItem="{Binding SelectedContact}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="8">
                    <Button Content="Add" Width="50" Height="25" Margin="3"
                        Command="{Binding AddCommand}"/>
                    <Button Content="Edit" Width="50" Height="25" Margin="3"
                        Command="{Binding EditCommand}" IsEnabled="{Binding IsEditing, Converter={StaticResource BooleanInverse}}"/>
                    
                    <Button Content="Remove" Width="50" Height="25" Margin="3"
                        Command="{Binding RemoveCommand}" IsEnabled="{Binding IsEditing, Converter={StaticResource BooleanInverse}}"/>
                </StackPanel>
            </Grid>

            <GridSplitter Grid.Column="1" Width="3" Background="White" HorizontalAlignment="Stretch"/>

            <Grid Grid.Column="2" Background="White">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid Margin="0, 43, 0, 0">
                    <Image Width="100" Height="100" VerticalAlignment="Top" Source="/contacts_96x96.ico"/>
                </Grid>

                <StackPanel Grid.Column="1" Margin="8,8,8,8">
                    <Label Content="Name:" Margin="0,3,0,3"/>
                    <TextBox Text="{Binding SelectedContact.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,3,0,3" IsReadOnly="{Binding IsEditing, Converter={StaticResource BooleanInverse}}"/>
                    <Label Content="Phone Number:" Margin="0,3,0,3"/>
                    <TextBox Text="{Binding SelectedContact.Number, UpdateSourceTrigger=PropertyChanged}" Margin="0,3,0,3" IsReadOnly="{Binding IsEditing, Converter={StaticResource BooleanInverse}}"/>
                    <Label Content="Email:" Margin="0,3,0,3"/>
                    <TextBox Text="{Binding SelectedContact.Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,3,0,3" IsReadOnly="{Binding IsEditing, Converter={StaticResource BooleanInverse}}"/>
                </StackPanel>

                <Button Grid.Column="1" Content="Apply" Width="75" Height="25" Margin="8,50,8,8" 
                    HorizontalAlignment="Right" Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibility}}" Command="{Binding ApplyCommand}"/>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
